* Use this file for MQSC that you want to be able to update without restarting the queue manager.

* Use a different dead letter queue, for undeliverable messages
DEFINE QLOCAL('~QM~.DLQ') REPLACE
ALTER QMGR DEADQ('~QM~.DLQ')
DEFINE CHANNEL('~QM~.1.SVRCONN') CHLTYPE(SVRCONN) SSLCAUTH(OPTIONAL) SSLCIPH('TLS_RSA_WITH_AES_256_GCM_SHA384') REPLACE
DEFINE CHANNEL('~QM~.2.SVRCONN') CHLTYPE(SVRCONN) SSLCAUTH(OPTIONAL) SSLCIPH('TLS_RSA_WITH_AES_256_GCM_SHA384') REPLACE

ALTER QMGR CHLAUTH (DISABLED)
REFRESH SECURITY TYPE(CONNAUTH)